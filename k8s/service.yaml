apiVersion: v1
kind: Service

metadata:
  name: springboot-service
  labels:
    app: springboot-app

spec:
  selector:
    app: springboot-app

  ports:
    - name: http
      port: 9091
      targetPort: 9091

  type: NodePort
